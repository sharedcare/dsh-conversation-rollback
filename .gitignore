node_modules/
*.tgz
.DS_Store
*.log

Thumbs.db
desktop.ini

# lib/client.js is generated by tools/merge-client.mjs and tracked on purpose:
# the package declares no `prepare` script, so a `github:` install must find the
# merged bundle already built. Never ignore lib/.
